//
//  TutorialManager.swift
//  Marble Tilt
//
//  Created by Scott Mayhew on 2025-12-04.
//


// TutorialManager.swift
import SpriteKit

class TutorialManager {
//    private(set) var stage: TutorialStage = .tiltCircle

    func start(in scene: GameScene) {
        // move the tutorial code you had (startTutorial, showNextStage, etc.)
        // For now we call the old startTutorial on the scene (keeps behavior identical)
//        scene.tutorialMode = true
//        scene.startTutorial()
    }

    func completeStage() {
        // update internal state & callbacks
    }
}
